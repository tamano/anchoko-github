# frozen_string_literal: true

RSpec.describe Anchoko::Github do
  it "has a version number" do
    expect(Anchoko::Github::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
